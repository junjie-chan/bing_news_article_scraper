<div class="row justify-content-center">
    {% for item in data %}
    <div class="col-3">
        <button class="freshness_buttons" onclick="toggle_freshness(this)" id="{{item}}">
            {{item}}
        </button>
    </div>
    {% endfor %}
</div>