<ul class="pagination">
    {% for tag in data %}
    <li
        class="page-item page_{{loop.index}} {{ 'active' if loop.index==2 else '' }} {{ 'disabled' if tage=='ellipsis' else '' }} index{{loop.index}}">
        <a class="page-link">{{'...' if tag=='ellipsis' else tag}}</a>
    </li>
    {% endfor %}
</ul>